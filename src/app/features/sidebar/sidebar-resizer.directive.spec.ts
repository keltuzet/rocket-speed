import { SidebarResizerDirective } from './sidebar-resizer.directive';

describe('SidebarResizerDirective', () => {
  it('should create an instance', () => {
    // const directive = new SidebarResizerDirective();
    // expect(directive).toBeTruthy();
  });
});
