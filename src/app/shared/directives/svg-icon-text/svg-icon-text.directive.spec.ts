import { SvgIconTextDirective } from './svg-icon-text.directive';

describe('SvgIconTextDirective', () => {
  it('should create an instance', () => {
    // const directive = new SvgIconTextDirective();
    // expect(directive).toBeTruthy();
  });
});
