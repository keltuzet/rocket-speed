import { LayoutForDirective } from './layout-for.directive';

describe('LayoutForDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutForDirective();
    expect(directive).toBeTruthy();
  });
});
