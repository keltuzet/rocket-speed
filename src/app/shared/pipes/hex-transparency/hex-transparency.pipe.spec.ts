import { HexTransparencyPipe } from './hex-transparency.pipe';

describe('HexTransparencyPipe', () => {
  it('create an instance', () => {
    const pipe = new HexTransparencyPipe();
    expect(pipe).toBeTruthy();
  });
});
