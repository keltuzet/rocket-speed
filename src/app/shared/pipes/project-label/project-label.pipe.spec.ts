import { ProjectLabelPipe } from './project-label.pipe';

describe('ProjectLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
